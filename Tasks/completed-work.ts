/**
 * 1. User + Patient Routes --- > first patient create, single get, all get (User + Patient), single update, single delete.
 *
 * 2. Admin Routes --- > admin create, single get, all get (Admin), single update, single delete.
 *
 *3. Doctor Routes --- > doctor create, single get, all get (Doctor), single delete, AI suggestion.
 * single update Problem........ doctor service page show 1 error
 *
 *
 *4. Schedule Routes --- > create schedule, get all schedules,, delete single schedule.
 *
 *
 *5. Specialties Routes --- > create specialty, get all specialties, delete specialty.
 *
 *
 *
 *6. Doctor Schedule Routes --- > create doctor schedule, get my schedule, delete doctor schedule, get all doctor schedules.      ( REad code all )
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 */
